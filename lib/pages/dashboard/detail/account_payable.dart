import 'package:flutter/material.dart';

class AccountPayable extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('account payable'),
    );
  }
}